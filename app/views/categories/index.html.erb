<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Categories</title>
  <style>
    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f7f7f7;
      margin: 0;
      padding: 20px;
      color: #333;
    }

    h2 {
      text-align: center;
      margin-bottom: 20px;
      color: #007bff;
    }

    .category-list {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
    }

    .category-card {
      background-color: #fff;
      border: 1px solid #ddd;
      border-radius: 8px;
      overflow: hidden;
      width: 300px;
      transition: box-shadow 0.3s;
    }

    .category-card:hover {
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .category-content {
      padding: 20px;
    }

    .category-title {
      font-size: 20px;
      font-weight: bold;
      color: #333;
      text-decoration: none;
      transition: color 0.3s;
    }

    .category-title:hover {
      color: #007bff;
    }

    .category-description {
      color: #666;
      margin-top: 10px;
    }

    .add-category-link {
      display:block;
      width: 300px;
      margin: 20px auto;
      padding: 10px 10px;
      background-color: #007bff;
      color: #fff;
      text-align: center;
      text-decoration: none;
      border-radius: 4px;
      transition: background-color 0.3s;
    }

    .add-category-link:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>

  <h2>Listing Categories: <%= @categories.size %></h2>

  <% if @categories.empty? %>
    <p>No categories added yet.</p>
  <% else %>
    <div class="category-list">
      <% @categories.each do |category| %>
        <div class="category-card">
          <div class="category-content">
            <a class="category-title" href="<%= category_path(category.id) %>">
              <%= category.name %>
            </a>
            </div>
        </div>
      <% end %>
    </div>
  <% end %>
  <% if user_signed_in? && current_user.is_admin %>
  <%= link_to "Add Category", new_category_path, class: "add-category-link" %>
  <% end %>


</body>

</html>
